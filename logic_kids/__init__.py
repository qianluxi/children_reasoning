"""儿童逻辑推理训练软件 —— 核心引擎。

架构（对齐专家意见）：
    题目内容层 -> 逻辑模型层 -> 自动验证层 -> 难度评估层
    -> 题库层 -> 出题策略层 -> 儿童交互层

核心原则：
    1. 文字只是表现层，Logic DSL 表达式才是核心。
    2. Solver 是整个系统的裁判：只有唯一解的题目才能入库。
    3. AI/人工负责创造题目，程序负责证明。
"""

__version__ = "0.1.0"
